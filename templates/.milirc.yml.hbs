{{#with (head mili.stack)}}
{{#if (equals type "npm")}}
template: npm:{{name}}
{{#if registry}}
registry: {{registry}}
{{/if}}
{{/if}}
{{#unless (equals type "npm")}}
template: {{name}}
{{/unless}}
version: {{version}}
{{/with}}
{{#unless (isEmpty answers)}}
answers:
{{#each answers}}
  {{@key}}: {{this}}
{{/each}}
{{/unless}}
